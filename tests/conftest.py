
# Keep the original M3 determinism test byte-identical for its immutable manifest.
# The portable successor executes every original assertion plus payload checks.
collect_ignore = ["test_real_historical_m3_source_package.py"]
